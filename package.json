{
  "name": "sideburns",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "sidequester"
  ],
  "scripts": {
    "dev": "npm run dev -w sidequester",
    "build": "npm run build -w sidequester",
    "preview": "npm run preview -w sidequester",
    "ios": "npm run ios -w sidequester",
    "cap:sync": "npm run cap:sync -w sidequester"
  }
}
