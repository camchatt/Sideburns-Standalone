-- Local-only seed. Safe defaults; no production credentials.
-- Official pack catalog stays empty until an explicit publish workflow exists.
-- User tables are intentionally unseeded so RLS ownership tests stay honest.

select 1;
